{
  "name": "startup-incubation-app",
  "version": "1.0.0",
  "description": "Complete MERN Startup Incubation Management Application",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run prod --prefix backend\" \"npm start --prefix frontend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "chai": "^5.1.2",
    "concurrently": "^7.0.0",
    "mocha": "^11.0.1"
  },
  "author": "SIMS Team",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.8",
    "react-bootstrap": "^2.10.10",
    "react-bootstrap-sidebar-menu": "^2.0.3",
    "react-scripts": "^5.0.1"
  }
}
